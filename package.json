{
  "name": "personal-website",
  "type": "module",
  "version": "1.0.0",
  "description": "winsten.dev personal website",
  "private": true,
  "license": "MIT",
  "engines": {
    "node": ">=22.12.0"
  },
  "cloudflare": {
    "label": "Personal Website",
    "products": [
      "Workers"
    ],
    "publish": false
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro build && wrangler dev",
    "astro": "astro",
    "cf-typegen": "wrangler types",
    "check": "astro build && tsc && wrangler deploy --dry-run",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^14.1.1",
    "@astrojs/mdx": "^7.0.2",
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^7.0.6",
    "typescript": "5.8.3"
  },
  "devDependencies": {
    "wrangler": "^4.108.0"
  }
}
